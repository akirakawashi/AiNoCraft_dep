# name: Deploy (self-hosted)

# on:
#   push:
#     branches: ["main"]
#   workflow_dispatch:

# concurrency:
#   group: deploy-main
#   cancel-in-progress: true

# jobs:
#   deploy:
#     runs-on: [self-hosted, linux]
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0

#       - name: Reset workspace to trigger SHA
#         shell: bash
#         run: |
#           set -euo pipefail

#           git fetch --prune origin
#           git reset --hard "$GITHUB_SHA"
#           git clean -fdx

#       - name: Deploy (docker compose)
#         shell: bash
#         run: |
#           set -euo pipefail

#           docker compose up -d --no-recreate nginx databasus postgres redis minio

